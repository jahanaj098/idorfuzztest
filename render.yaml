services:
  - type: web
    name: idor-test-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn vulnerable_idor_app:app
    envVars:
      - key: FLASK_ENV
        value: production
